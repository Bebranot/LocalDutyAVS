<Control xmlns="https://spacestation14.io" MinWidth="300" MaxWidth="620">
    <PanelContainer StyleClasses="BackgroundPanel" />
    <BoxContainer Margin="4" Orientation="Vertical">
        <Label Name="VoteCaller" />
        <RichTextLabel Name="VoteTitle" />
        <Button Margin="4 4" Name="FollowVoteTarget" Text="{Loc 'ui-vote-follow-button-popup'}" Visible="False"></Button>

        <!-- Size-limited grid: column count is derived from the widest option so the
             window grows to fit the text and every button shares one uniform width
             instead of overflowing its bounds. See VotePopup.MaxGridWidth setup. -->
        <GridContainer Name="VoteOptionsContainer" HorizontalExpand="True"/>
        <BoxContainer Orientation="Horizontal">
            <ProgressBar Margin="4" HorizontalExpand="True" Name="TimeLeftBar" MinValue="0" MaxValue="1" />
            <Label Name="TimeLeftText" />
        </BoxContainer>
    </BoxContainer>
</Control>
